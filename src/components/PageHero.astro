---
import type { AsciiArt } from '@data/ascii-art';
import type { SocialItem } from '@data/social';
import ExternalLinkButton from './ExternalLinkButton.astro';

export interface Props {
  title: string;
  intro?: string;
  asciiArt?: AsciiArt;
  socialItems?: SocialItem[];
  asciiPosition?: 'left' | 'right';
  asciiFlushTop?: boolean;
  asciiFontSize?: 'xs' | 'sm' | 'base';
}

const {
  title,
  intro,
  asciiArt,
  socialItems = [],
  asciiPosition = 'right',
  asciiFlushTop = false,
  asciiFontSize = 'sm',
} = Astro.props;
---

<section class:list={['page-hero', `page-hero--ascii-${asciiPosition}`]}>
  <div class="page-hero__content">
    <h1 class="page-hero__title">{title}</h1>
    {intro && <p class="page-hero__intro">{intro}</p>}
    {socialItems.length > 0 && (
      <div class="page-hero__social">
        {socialItems.map((item) => (
          <ExternalLinkButton {...item} />
        ))}
      </div>
    )}
    <slot />
  </div>
  {asciiArt && (
    <div class:list={['page-hero__ascii-wrapper', { 'page-hero__ascii-wrapper--flush-top': asciiFlushTop }]}>
      <pre
        class:list={['page-hero__ascii', `page-hero__ascii--size-${asciiFontSize}`]}
        role="img"
        aria-label={asciiArt.alt}
      >{asciiArt.art}</pre>
    </div>
  )}
</section>

<style>
  .page-hero {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: var(--spacing-3xl);

    @media (max-width: 768px) {
      flex-direction: column;
      align-items: center;
    }
  }

  .page-hero--ascii-left {
    flex-direction: row-reverse;

    @media (max-width: 768px) {
      flex-direction: column;
    }
  }

  .page-hero__content {
    flex: 1;
    max-width: 60ch;
  }

  .page-hero__title {
    font-size: var(--font-size-3xl);
  }

  .page-hero__intro {
    font-size: var(--font-size-lg);
    line-height: 1.6;
  }

  .page-hero__social {
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-md);
    margin-top: var(--spacing-md);

    @media (max-width: 768px) {
      justify-content: center;
    }
  }

  .page-hero__ascii-wrapper {
    flex: 0 0 35%;

    @media (max-width: 768px) {
      flex: none;
      width: 100%;
      max-width: 300px;
    }
  }

  .page-hero__ascii {
    font-family: var(--font-family-mono);
    font-size: var(--font-size-sm);
    line-height: 1;
    white-space: pre;
    color: var(--color-secondary);
    overflow-x: auto;
    overflow-y: clip;
    font-variant-ligatures: none;
  }

  .page-hero__ascii-wrapper--flush-top {
    @media (min-width: 769px) {
      margin-top: calc(-1 * var(--spacing-3xl));
    }
  }

  .page-hero__ascii--size-xs {
    font-size: var(--font-size-xs);
  }

  .page-hero__ascii--size-base {
    font-size: var(--font-size-base);
  }
</style>
